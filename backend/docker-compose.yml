services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: savora
      POSTGRES_USER: savora
      POSTGRES_PASSWORD: savora
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
